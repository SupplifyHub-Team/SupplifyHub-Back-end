namespace Enum
{
    public enum OrderStatus
    {
        Active = 0, // Order is Active
        InProgress = 1, // Order is being InProgress
        Completed = 2, // Order completed successfully
        Canceled = 3, // Order was Failed
    }
}
