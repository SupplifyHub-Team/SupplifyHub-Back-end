namespace Enum
{
    public enum RoleName
    {
        Admin = 0, // Admin role
        Suppliers = 1, // Suppliers role
        Clients = 2, // Clients role
        JobSeeker = 3, // Job Seeker role
    }
}
